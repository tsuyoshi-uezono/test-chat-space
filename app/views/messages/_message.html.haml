.message{"data-message_id": message.id}
  .message__upper-info
    %p.message__upper-info__talker= message.user.name
    %p.message__upper-info__date= message.created_at.to_s
  %p.message__text= message.content
  = image_tag "#{message.image}" if message.image.present?
